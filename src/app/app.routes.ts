import { Routes } from '@angular/router';

export const routes: Routes = [

  //{ path: 'record/:id', component: BusinessEntityRecordMainComponent},

  // { path: 'codegen', loadChildren: () => import('src/app/utility/codegen/codegen.module').then(m => m.CodeGenModule) },

  // { path: '', redirectTo: 'authen/signin', pathMatch: 'full' }, //pathMatch: 'full' must be here for empty path.
  // { path: '**', redirectTo: 'authen/signin' }
];
